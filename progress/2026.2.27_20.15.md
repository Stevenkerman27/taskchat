### 2026.2.27 任务进度总结

**任务目标：**
1. 修复 API 密钥调用方式（从 YAML 改为环境变量）。
2. 修复 GUI 中文输入不支持的问题。
3. 解决 GUI 双击运行闪退的问题。
4. 在 GUI 中增加供应商和模型选择，并接入 Gemini。

**执行状态：**
- **API 密钥修复**：修改 `chat_logic.py`，使用 `os.getenv` 从系统变量中读取密钥。
- **中文支持修复**：将 GUI 字体统一更换为 `Microsoft YaHei`，解决部分环境下中文输入及显示异常，并在 JSON 预览中禁用了 ASCII 转义。
- **运行稳定性修复**：
    - 在 `chat_logic.py` 中改用绝对路径加载配置文件，解决双击运行时因工作目录不一致导致的找不到文件问题。
    - 在 `chat_gui.py` 中增加了全局异常捕获，若程序启动失败将弹出 `messagebox` 提示具体错误原因，不再无声闪退。
- **功能增强**：
    - GUI 顶部新增 Provider 和 Model 下拉选择框。
    - `chat_logic.py` 成功接入 `google-genai` 库，支持 Gemini 系列模型（如 `gemini-3-flash-preview`）。
    - 优化了 Payload 预览逻辑，实时显示完整请求结构。

**测试建议：**
- 确保已设置环境变量 `DS_API_KEY` 和 `GEMINI_API_KEY`。
- 双击 `chat_gui.py` 验证启动及中文输入功能。
