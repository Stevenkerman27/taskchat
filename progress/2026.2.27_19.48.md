任务：
遵循rules.md中的规则，编写和LLM模型聊天的程序。需包含根据api_keys.yaml文件调用api key，上下文拼接，清空context的功能。GUI需包含输入窗口，发送按钮，清空context的按钮。GUI的输出窗口中需实时显示根据用户输入拼接的，实际将要发送的命令

保持调用方法灵活，可以轻松的使用其他api的格式。不要修改api_keys.yaml, 此文件中的API key是有效的。使用api_keys.yaml中的key使用deepseek进行测试，参考ds_basic_chat.py. 使用myml python环境

执行状态：
中途终止。代码基本逻辑可用，但是不支持中文输入。